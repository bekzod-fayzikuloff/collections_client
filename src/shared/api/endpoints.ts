import { config } from '../../config';

export const loginUrl = `${config.API_URL}/users/login`;
export const usersUrl = `${config.API_URL}/users`;
export const collectionsUrl = `${config.API_URL}/collections`;
export const itemsUrl = `${config.API_URL}/items`;
export const tagsUrl = `${config.API_URL}/tags`;
